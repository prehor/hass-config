---
# The http component serves all files and data required for the
# Home Assistant frontend.
#
# https://www.home-assistant.io/components/http/
#

http:
  trusted_proxies:
    - !env_var hass_trusted_proxy_1
  use_x_forwarded_for: true
